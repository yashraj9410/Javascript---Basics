// myscript.js

// Select the div with the ID "exampleDiv"
var exampleDiv = document.getElementById('exampleDiv');

// Set the content using innerHTML (includes HTML markup)
exampleDiv.innerHTML = '<h1>This is inner HTML</h1>';

// Set the content using textContent (treats content as plain text)
//exampleDiv.textContent = '<strong>This is <em>textContent</em></strong>';
